Ansible playbook for test.\
If you have to start it, your need install ansible before.

For example simple\
installation with virtualenv:

mkdir ansible\
virtualenv ansible\
source ansible/bin/activate\
pip3 install ansible

NOTE:\
do not use this for production currently
